<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="es.unizar.eina.M132_quads.ui.QuadList">

    <!-- FRANJA SUPERIOR PARA EL TÍTULO -->
    <!-- Creamos una vista simple que servirá como cabecera. -->
    <TextView
        android:id="@+id/header_title"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:text="@string/listado_de_quads"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Headline6"
        android:background="?attr/colorPrimary"
        android:textColor="?attr/colorOnPrimary"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- TEXTVIEW PARA LA ETIQUETA "Ordenar por" -->
    <TextView
        android:id="@+id/label_ordenar_por"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ordenar por:"
        android:textAppearance="?attr/textAppearanceBody1"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toTopOf="@+id/spinner_filter"
        app:layout_constraintBottom_toBottomOf="@+id/spinner_filter"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- SPINNER PARA FILTRAR/ORDENAR (MODIFICADO) -->
    <!-- Ahora empieza a la derecha del TextView -->
    <Spinner
        android:id="@+id/spinner_filter"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="7dp"
        android:layout_marginTop="25dp"
        android:minHeight="48dp"
        android:entries="@array/opciones_ordenar_quads"
        app:layout_constraintTop_toBottomOf="@id/header_title"
        app:layout_constraintStart_toEndOf="@+id/label_ordenar_por"
        app:layout_constraintEnd_toEndOf="parent" />



    <!-- BARRA DE NAVEGACIÓN INFERIOR (BottomNavigationView) -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:itemIconTint="@color/bottom_nav_icon_tint"
        app:itemTextColor="@color/bottom_nav_icon_tint"
        app:menu="@menu/bottom_navigation_menu"
        app:itemActiveIndicatorStyle="@style/App.BottomNavigationView.ActiveIndicator"
        app:labelVisibilityMode="labeled"
        android:background="?attr/colorPrimary" />



    <!-- RecyclerView -->
    <!-- Se ancla entre la cabecera (header_title) y la barra de navegación (bottom_navigation). -->
    <!-- El alto "0dp" significa le indica que ocupe todo el espacio disponible entre las restricciones". -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerview"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="@dimen/big_padding"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/spinner_filter"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/recyclerview_item" />

    <!-- FloatingActionButton (FAB) -->
    <!-- Se ancla al borde inferior de la RecyclerView y al borde final de la pantalla. -->
    <!-- Así, se moverá junto con la lista y quedará justo encima de la barra de navegación. -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:contentDescription="@string/add_quad"
        android:src="@drawable/ic_add_black_24dp"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintEnd_toEndOf="parent"
        app:backgroundTint="@color/darkBlue"
        app:tint="@color/white" />

</androidx.constraintlayout.widget.ConstraintLayout>
